<%= form_for(current_muser.active_subscribes.build, remote: true) do |f| %>
<%= form_for(current_muser.active_subscribes.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @muser.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>