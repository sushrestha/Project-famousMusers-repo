<% provide(:title, @title) %>
<div class="row">
  <div class="col-md-12">
    <% @following.each do |fol| %>
      <% fol.musings.each do |musing| %>
        <% if musing.isPrivate == 0 %>
          <li> <%= musing.content %> - <%= link_to fol.name, "/musers/#{fol.id}/" %><br /></li>
        <% end %>
      <% end %>
    <% end %>
  </div>
</div>