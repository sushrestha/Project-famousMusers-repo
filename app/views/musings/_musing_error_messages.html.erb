<% if @musing.errors.any? %>

  <div class="alert alert-danger">
    The form contains <%= pluralize(@musing.errors.count, "error") %>:
  </div>

  <ul>
    <% @musing.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>

<% end %>